STATE switch_stmt
    IF p[0]['exp'][0] EQUALTO "switch"
        IF "default" NOTIN p[0]['exp']
            PRINT LINE
            VIOLATION "switch" statements should end with "default" clauses
        END_IF  
    END_IF
END_STATE
int main()
{
    // variable declaration used for indexes
    int i, j, k, l, size;
  
    // Array declaration
    //int a[2][2][2][2];
  
    // size of array
    size = 2;
  
    // elements input
  
    // Printing the values
    for (i = 0; i < size; i++) {
        for (j = 0; j = size; j++) {
            for (k = 0; k = size; k++) {
                for (l = 0; l < size; l++) {
                    //printf("Value of a[%d][%d][%d][%d] :- %d ", 
                      //             i, j, k, l, a[i][j][k][l]);
                    printf("yayy!!");
                }
            }
        }
    }
    return 0;
}
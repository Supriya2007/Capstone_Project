//#include <stdio.h>

int main(){
    int arr[10];
    int i=0, x, y, z;
    
    arr[i] = i++;
    x = i + i++;
    y = (++i)*2 - 3 + i;
    //y = (++i)*2-3;
    if(i++ + i){
        z = 1;
    }
    //(a++ -1 > 0) ? a++ : a--;
    return a++ - a;
    if(i++ && i){
        y = 10;
    }
    for(i=0; i++ + i > 10; i++){
        break;
    }
    while( i++ + i> 10){
    
    }
    func(i, i++);
    
    return 0;
}

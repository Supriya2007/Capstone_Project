int main(){
    int a, b, c, sum, double_b;
    int d = 10, e = 5;
    int diff;
    b = 10;
    sum = a+b;
    sum = a+c;
    double_b = b*2;
    a = 10;
    diff = a-c;
    
    a+c;
    b*2*a*c;
    b*2+a+c;
    
    ++a; ++c;
    sizeof(a); sizeof(c);
    a++; c++;
    d++; ++b;
    
    /*
    Not tested since attributed grammar for control statements is yet to be done.
    if(diff>c){ // Will assume c is initialized probably :( 
        c = a-b;
    }
    else{
        diff -= c;
    }
    */
}
